# 后端当前进度

## 当前状态

- 当前分支：`main`
- 后端代码已合并并推送到 GitHub `main`
- 当前后端已完成前端 P0 接口闭环
- 当前数据库使用正式三张核心表：
  - `smoke_device`
  - `smoke_data`
  - `alarm_record`
- 已关闭 JPA 自动修改表结构：`spring.jpa.hibernate.ddl-auto: none`

## 已完成后端能力

1. Spring Boot 3 + Maven 后端项目骨架。
2. Java 分层结构：
   - `controller`：接收请求、返回 `ApiResult`
   - `service`：业务逻辑
   - `repository`：数据库查询
   - `entity`：正式表映射
   - `dto`：接口请求和响应对象
   - `common`：统一响应和异常处理
3. 正式表 Entity 映射：
   - `Device` -> `smoke_device`
   - `SensorData` -> `smoke_data`
   - `AlarmRecord` -> `alarm_record`
4. P0 查询接口：
   - 系统状态
   - 设备状态
   - 最新烟雾状态
   - 历史烟雾趋势
   - 今日告警统计
   - 告警日志列表
5. P0 演示接口：
   - 模拟烟雾升高
   - 恢复正常环境
   - 模拟控制蜂鸣器、报警灯、排风扇
6. 历史烟雾趋势已支持 `source` 过滤，避免模拟数据污染真实历史。
7. 设备离线判断：
   - 使用 `smoke_device.last_heartbeat` 判断设备是否在线
   - 超过 60 秒未上报或心跳为空时，认为设备离线
   - 离线时最新烟雾接口不再返回旧烟雾值
8. 前端运行态展示接口：
   - 运行连接快照
   - 实时烟雾展示
   - 设备基础信息
9. 登录鉴权（基于 `sys_user`）：
   - 账号密码登录 `POST /api/auth/login`
   - 注册 `POST /api/auth/register`（成功后直接返回 token）
   - 发送短信验证码 `POST /api/auth/sms-code`（演示环境把验证码打到后端控制台日志）
   - 短信验证码登录 `POST /api/auth/sms-login`
   - 获取当前用户资料 `GET /api/auth/me`

## 已实现 P0 接口

| 接口 | 说明 | 主要数据表 |
|---|---|---|
| `GET /api/system/status` | 查询系统在线状态和设备数量 | `smoke_device` |
| `GET /api/device/status` | 查询设备在线、心跳和状态 | `smoke_device` |
| `GET /api/smoke/latest` | 查询最新烟雾值、风险等级、告警状态 | `smoke_device`、`smoke_data` |
| `GET /api/smoke/history` | 查询烟雾历史趋势 | `smoke_data` |
| `GET /api/alarm/stat/today` | 查询今日/昨日告警统计 | `alarm_record` |
| `GET /api/alarm/logs` | 查询告警日志列表 | `alarm_record` |
| `POST /api/smoke/simulate` | 模拟烟雾升高并生成数据/告警 | `smoke_device`、`smoke_data`、`alarm_record` |
| `POST /api/smoke/restore` | 恢复正常环境并解除未处理告警 | `smoke_device`、`smoke_data`、`alarm_record` |
| `POST /api/device/control` | 写入联动控制状态，供设备模块发布 MQTT 控制消息 | `smoke_device`、`device_control` |
| `POST /api/auth/login` | 账号密码登录，返回 token + 用户资料 | `sys_user` |
| `POST /api/auth/register` | 注册新账号，成功后返回 token | `sys_user` |
| `POST /api/auth/sms-code` | 生成并缓存 6 位短信验证码 | `sys_user` |
| `POST /api/auth/sms-login` | 短信验证码登录 | `sys_user` |
| `GET /api/auth/me` | 获取当前登录用户完整资料 | `sys_user` |

## 新增前端展示接口

| 接口 | 说明 | 主要数据表 |
|---|---|---|
| `GET /api/runtime/link-snapshot` | 查询页面启动时的连接快照，决定展示仪表盘还是未连接页 | `smoke_device` |
| `GET /api/smoke/realtime` | 查询前端实时烟雾展示数据 | `smoke_device` |
| `GET /api/device/info` | 查询设备基础信息 | `smoke_device` |

## 设备离线判断规则

当前离线超时时间来自数据库配置表：

```text
system_setting.heartbeat_timeout
```

当前 `dream28.system_setting` 中配置值为：

```text
heartbeat_timeout = 300
```

判断规则：

- `lastHeartbeat` 为空：离线
- 当前时间与 `lastHeartbeat` 相差超过 `heartbeat_timeout` 秒：离线
- `heartbeat_timeout` 秒内有上报：在线

离线时：

- `GET /api/smoke/latest` 返回 `connected = false`、`smokeValue = null`、`riskLevel = unknown`、`alarmStatus = offline`
- `GET /api/smoke/realtime` 返回 `connected = false`、`smokeValue = null`、`themeType = offline`
- `GET /api/device/status` 返回 `connected = false`、`status = offline`
- `GET /api/system/status` 的 `onlineDeviceCount` 按 60 秒内心跳统计

## 前端联调注意事项

- `GET /api/runtime/link-snapshot` 可作为页面打开后的首个接口，用于判断是否进入未连接页。
- 当前 MQTT 未由本模块接入，`mqttOnline` 暂时跟随设备心跳在线状态，不代表真实 broker 连接状态。
- `GET /api/smoke/realtime` 是前端展示友好的实时接口，和 `GET /api/smoke/latest` 不冲突。
- `temperature`、`humidity` 当前返回 `null`，因为正式三张表中没有温湿度字段。
- `GET /api/device/info` 中 `model`、`firmwareVersion` 当前返回 `null`，因为正式设备表中没有对应字段。
- 如果前端支持多设备，建议始终传 `deviceId`，不要依赖后端自动选择最近更新设备。

## 历史趋势 source 规则

`simulate` 和 `restore` 会写入 `smoke_data`，但写入来源为：

```text
source = simulate
```

真实硬件上传数据来源应为：

```text
source = sensor
```

历史趋势接口默认只返回真实数据：

```http
GET /api/smoke/history?deviceId=SMK-001&range=24h
```

等价于：

```http
GET /api/smoke/history?deviceId=SMK-001&range=24h&source=sensor
```

如果要查看模拟数据：

```http
GET /api/smoke/history?deviceId=SMK-001&range=24h&source=simulate
```

如果要查看全部数据：

```http
GET /api/smoke/history?deviceId=SMK-001&range=24h&source=all
```

## 当前注意事项

- 不要随意修改远程 MySQL 表结构。
- 不要把 `ddl-auto` 改为 `update`、`create` 或 `create-drop`。
- `simulate` 和 `restore` 是演示/联调用接口，硬件真实上传稳定后不应作为正式数据来源。
- `device/control` 当前负责写入 `device_control` 表；真实 MQTT 发布由 `device/postData` 模块轮询数据库后完成。
- 控制接口 HTTP 字段使用 `target`、`action`，数据库仍使用 `control_type`、`status`。
- `device/connect`、`device/disconnect` 暂未实现，等 MQTT/硬件连接方案确定后再接入。
- `runtime/link-events` 暂未实现 SSE，当前前端可先轮询 `runtime/link-snapshot`。
- `docs/frontend-api-list.xlsx` 当前是本地未跟踪文件，未提交到 GitHub。

## 2026-07-04 后端维护记录

本次维护重点是提升代码可读性和构建稳定性，没有修改数据库表结构。

已完成：

1. 补充 Controller、Service、ServiceImpl、Repository、Entity、DTO、Common 层的中文说明注释。
2. 清理历史乱码注释和乱码返回文案。
3. 将常见英文错误提示统一为中文提示，例如：
   - `设备不存在`
   - `未找到设备`
   - `告警不存在`
   - `告警已处理`
4. 修复 Java 文件 UTF-8 BOM 问题，避免 `mvn clean package` 出现：

```text
非法字符: '\ufeff'
```

5. 当前后端已通过：

```text
mvn package
```

注意：

- `mvn clean package` 如果失败在 `target/test-classes` 删除阶段，通常是 Windows 文件占用问题，不是源码编译问题。
- 遇到该情况可以先关闭正在运行的后端、VS Code Java 进程或占用 `target` 的程序，再重新执行。
- 若只是验证源码能否编译打包，可以先执行 `mvn package`。

