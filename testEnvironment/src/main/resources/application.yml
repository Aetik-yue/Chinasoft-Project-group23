server:
  port: 8099

spring:
  application:
    name: environment-check
  datasource:
    url: ${MYSQL_URL:jdbc:mysql://47.108.58.107:3306/dream26?useUnicode=true&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia%2FShanghai}
    username: ${MYSQL_USERNAME:root}
    password: ${MYSQL_PASSWORD:}
    hikari:
      connection-timeout: 10000
      maximum-pool-size: 2
      minimum-idle: 0
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      connect-timeout: 5s
      timeout: 5s

environment-check:
  expected-database: dream26
  mqtt-broker: ${MQTT_BROKER:tcp://47.108.58.107:1883}
  maxkb-url: ${MAXKB_URL:http://localhost:8081}
  dataease-url: ${DATAEASE_URL:http://localhost:8100}
  timeout-seconds: 10

logging:
  level:
    root: INFO
    com.zaxxer.hikari: WARN
