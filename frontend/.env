# 团队共享：前端连后端的地址。改这里 git push，所有人 pull 后即生效。
# 谁跑后端（装了 YOLO+CLIP 模型的机器）就填谁的局域网 IP；本机跑填 localhost 也行。
# 主后端（下面填的，连同学有识别模型的后端）离线时，自动 fallback 到本地后端 localhost:8080
# （需本机也开着后端，用根目录 start-local.bat 启动）。
# 想临时强制只用本地：在 frontend/ 下建 .env.local（不入库）写 VITE_BACKEND_HOST=localhost。
# 自定义本地兜底地址：VITE_FALLBACK_HOST（默认 localhost）。
VITE_BACKEND_HOST=192.168.20.35
VITE_AMAP_KEY=886a6832b03aec713827d5b551d3486d
VITE_AMAP_SECURITY_CODE=70dbc244514f30bf14eefe0eae6ca8a2

# MaxKB 嵌入转发目标（vite 把浏览器 /chat/* 请求转发到这里）。
# 谁跑 MaxKB Docker 就填谁在【当前网络】的 IP；本机跑 MaxKB 填 localhost 也行。
# ⚠️ 换网络（热点/答辩现场 WiFi）后这台机器 IP 会变，这里要跟着改，否则聊天窗加载不出来。
# 当前：MaxKB 在我的电脑上，热点网段 IP = 192.168.94.244
VITE_MAXKB_HOST=192.168.94.244
VITE_MAXKB_PORT=18080
