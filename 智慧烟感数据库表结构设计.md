# 智慧烟感预警系统 - 数据库表结构设计

> 版本：v1.0
> 日期：2026-06-30
> 数据库：MySQL 8.0+
> 字符集：utf8mb4

---

## 0. 设计概览

| 序号 | 表名 | 中文名 | 说明 | 预估数据量 |
|------|------|--------|------|-----------|
| 1 | `sys_user` | 用户表 | 登录账号与权限 | 少量（<100） |
| 2 | `smoke_device` | 烟感设备表 | 设备基本信息与状态 | 少量（<1000） |
| 3 | `smoke_data` | 烟雾数据表 | 历史浓度数据，量最大 | 海量（按采样频率） |
| 4 | `alarm_record` | 告警记录表 | 告警事件主表 | 中等 |
| 5 | `alarm_timeline` | 告警时间线表 | 告警生命周期事件 | 中等 |
| 6 | `device_control` | 联动设备表 | 蜂鸣器/报警灯/排风扇等 | 少量 |
| 7 | `system_setting` | 系统设置表 | 阈值、全局配置 | 极少（KV形式） |
| 8 | `vision_check` | 视觉复核表 | AI摄像头复核结果 | 少量 |

---

## 1. 用户表 `sys_user`

用于系统登录与用户管理。原型左侧菜单有「用户管理」。

```sql
CREATE TABLE `sys_user` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `username` VARCHAR(64) NOT NULL COMMENT '用户名',
  `password` VARCHAR(128) NOT NULL COMMENT '加密密码（建议 bcrypt）',
  `real_name` VARCHAR(64) DEFAULT NULL COMMENT '真实姓名',
  `role` VARCHAR(32) NOT NULL DEFAULT 'viewer' COMMENT '角色：admin-管理员, viewer-只读用户',
  `phone` VARCHAR(20) DEFAULT NULL COMMENT '手机号',
  `email` VARCHAR(100) DEFAULT NULL COMMENT '邮箱',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态：1-启用, 0-禁用',
  `last_login_time` DATETIME DEFAULT NULL COMMENT '最后登录时间',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_username` (`username`),
  KEY `idx_role` (`role`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

**初始化数据（演示用）**

```sql
INSERT INTO `sys_user` (`username`, `password`, `real_name`, `role`) VALUES
('admin', '$2a$10$...', '管理员', 'admin'),
('viewer', '$2a$10$...', '观察员', 'viewer');
```

---

## 2. 烟感设备表 `smoke_device`

核心设备表。原型中「设备编号: SMOKE-001」「设备在线 12/12」均来自此表。

```sql
CREATE TABLE `smoke_device` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `device_id` VARCHAR(64) NOT NULL COMMENT '设备编号（业务唯一，如 SMK-001）',
  `name` VARCHAR(128) NOT NULL COMMENT '设备名称（如 1号仓库烟感）',
  `location` VARCHAR(255) DEFAULT NULL COMMENT '安装位置',
  `online` TINYINT NOT NULL DEFAULT 1 COMMENT '在线状态：1-在线, 0-离线',
  `last_heartbeat` DATETIME DEFAULT NULL COMMENT '最近心跳时间',
  `current_smoke_value` INT DEFAULT 0 COMMENT '当前烟雾浓度（ppm，冗余缓存最新值）',
  `current_risk_level` VARCHAR(32) DEFAULT 'normal' COMMENT '当前风险等级：normal/low/medium/high',
  `current_alarm_status` VARCHAR(32) DEFAULT 'safe' COMMENT '当前报警状态：safe/alarm/offline',
  `enabled` TINYINT NOT NULL DEFAULT 1 COMMENT '是否启用：1-启用, 0-禁用',
  `remark` VARCHAR(500) DEFAULT NULL COMMENT '备注',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_device_id` (`device_id`),
  KEY `idx_online` (`online`),
  KEY `idx_enabled` (`enabled`),
  KEY `idx_alarm_status` (`current_alarm_status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='烟感设备表';
```

**设计要点**

- `current_smoke_value`、`current_risk_level`、`current_alarm_status` 是**冗余字段**，用于首页快速查询，避免每次查历史数据表。由后端在收到 MQTT 数据时更新。
- `online` 状态由心跳检测服务定时更新（如 5 分钟未收到心跳则置为离线）。

**初始化数据**

```sql
INSERT INTO `smoke_device` (`device_id`, `name`, `location`, `current_smoke_value`, `current_risk_level`, `current_alarm_status`) VALUES
('SMK-001', '1号仓库烟感', 'A区1号仓库', 86, 'low', 'safe');
```

---

## 3. 烟雾数据表 `smoke_data`

历史浓度数据，量最大的表。用于「烟雾浓度历史趋势」折线图。

```sql
CREATE TABLE `smoke_data` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `device_id` VARCHAR(64) NOT NULL COMMENT '设备编号',
  `smoke_value` INT NOT NULL COMMENT '烟雾浓度（ppm）',
  `risk_level` VARCHAR(32) NOT NULL COMMENT '风险等级：normal/low/medium/high',
  `record_time` DATETIME NOT NULL COMMENT '数据记录时间（传感器上报时间）',
  `source` VARCHAR(32) DEFAULT 'sensor' COMMENT '数据来源：sensor-真实传感器, simulate-模拟',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '入库时间',
  PRIMARY KEY (`id`),
  KEY `idx_device_time` (`device_id`, `record_time`),
  KEY `idx_record_time` (`record_time`),
  KEY `idx_risk_level` (`risk_level`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='烟雾浓度历史数据表';
```

**设计要点**

| 要点 | 说明 |
|------|------|
| 索引策略 | `idx_device_time` 覆盖 99% 的查询（按设备+时间范围查历史） |
| 分区建议 | 数据量大时按 `record_time` 按月分区，或定时归档到 `smoke_data_archive` |
| 聚合查询 | 首页 24h/7d 趋势不要直接读原始数据，建议定时任务预聚合到 `smoke_data_hourly` |
| 数据保留 | 原始数据保留 30 天，聚合数据保留 1 年 |

**预聚合表（可选，推荐）**

```sql
CREATE TABLE `smoke_data_hourly` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `device_id` VARCHAR(64) NOT NULL,
  `hour` DATETIME NOT NULL COMMENT '整点小时（如 2026-06-30 14:00:00）',
  `avg_value` INT NOT NULL COMMENT '小时平均浓度',
  `max_value` INT NOT NULL COMMENT '小时最大浓度',
  `min_value` INT NOT NULL COMMENT '小时最小浓度',
  `sample_count` INT NOT NULL COMMENT '采样点数',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_device_hour` (`device_id`, `hour`),
  KEY `idx_hour` (`hour`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='烟雾浓度小时聚合表';
```

---

## 4. 告警记录表 `alarm_record`

告警事件主表。原型中「历史报警记录」「今日告警」均来自此表。

```sql
CREATE TABLE `alarm_record` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `alarm_id` VARCHAR(64) NOT NULL COMMENT '告警编号（业务唯一，如 ALM-20260630-001）',
  `device_id` VARCHAR(64) NOT NULL COMMENT '设备编号',
  `alarm_type` VARCHAR(64) NOT NULL COMMENT '告警类型：smoke_high-烟雾浓度超限, device_offline-设备离线',
  `smoke_value` INT DEFAULT NULL COMMENT '触发时浓度（ppm，离线告警为null）',
  `risk_level` VARCHAR(32) NOT NULL COMMENT '风险等级：low/medium/high',
  `status` VARCHAR(32) NOT NULL DEFAULT 'pending' COMMENT '处理状态：pending-待处理, processing-处理中, resolved-已处理',
  `handler` VARCHAR(64) DEFAULT NULL COMMENT '处理人用户名',
  `handled_at` DATETIME DEFAULT NULL COMMENT '处理时间',
  `remark` VARCHAR(1000) DEFAULT NULL COMMENT '处理备注',
  `triggered_at` DATETIME NOT NULL COMMENT '告警触发时间',
  `resolved_at` DATETIME DEFAULT NULL COMMENT '告警恢复时间（手动恢复或自动恢复）',
  `is_simulated` TINYINT NOT NULL DEFAULT 0 COMMENT '是否模拟触发：1-是, 0-否',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_alarm_id` (`alarm_id`),
  KEY `idx_device_id` (`device_id`),
  KEY `idx_status` (`status`),
  KEY `idx_triggered_at` (`triggered_at`),
  KEY `idx_alarm_type` (`alarm_type`),
  KEY `idx_composite` (`device_id`, `status`, `triggered_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='告警记录表';
```

**设计要点**

- `alarm_id` 生成规则建议：`ALM-YYYYMMDD-NNN`（如 `ALM-20260630-001`），也可用雪花算法。
- `status` 流转：pending → processing → resolved（单向，不可回退）。
- `is_simulated` 用于标记「模拟烟雾升高」触发的告警，便于演示后清理或统计区分。
- 今日告警统计 SQL 示例：

```sql
-- 今日告警次数（中风险及以上）
SELECT COUNT(*) FROM alarm_record 
WHERE triggered_at >= CURDATE() 
  AND triggered_at < DATE_ADD(CURDATE(), INTERVAL 1 DAY)
  AND risk_level IN ('medium', 'high');

-- 较昨日变化
SELECT 
  (SELECT COUNT(*) FROM alarm_record WHERE triggered_at >= CURDATE() AND risk_level IN ('medium','high')) AS today_count,
  (SELECT COUNT(*) FROM alarm_record WHERE triggered_at >= DATE_SUB(CURDATE(),INTERVAL 1 DAY) AND triggered_at < CURDATE() AND risk_level IN ('medium','high')) AS yesterday_count;
```

---

## 5. 告警时间线表 `alarm_timeline`

记录告警生命周期中的关键事件，用于「告警详情」中的时间线展示。

```sql
CREATE TABLE `alarm_timeline` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `alarm_id` VARCHAR(64) NOT NULL COMMENT '关联告警编号',
  `event_type` VARCHAR(64) NOT NULL COMMENT '事件类型：trigger-告警触发, linkage_start-联动启动, linkage_end-联动结束, handle-人工处理, auto_resolve-自动恢复, vision_check-视觉复核',
  `event_desc` VARCHAR(255) NOT NULL COMMENT '事件描述',
  `operator` VARCHAR(64) DEFAULT NULL COMMENT '操作人（系统触发为 system）',
  `extra_data` JSON DEFAULT NULL COMMENT '扩展信息（JSON格式，如 {"deviceType":"fan","status":"on"}）',
  `event_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '事件发生时间',
  PRIMARY KEY (`id`),
  KEY `idx_alarm_id` (`alarm_id`),
  KEY `idx_event_time` (`event_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='告警处理时间线表';
```

**设计要点**

- 与 `alarm_record` 是一对多关系。
- `extra_data` 用 JSON 类型存储灵活扩展信息（如联动了哪些设备、视觉复核结果等）。

---

## 6. 联动设备表 `device_control`

蜂鸣器、报警灯、排风扇等联动控制设备的状态。原型右侧「设备控制」面板。

```sql
CREATE TABLE `device_control` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `device_id` VARCHAR(64) NOT NULL COMMENT '所属烟感设备编号',
  `control_type` VARCHAR(64) NOT NULL COMMENT '控制设备类型：buzzer-蜂鸣器, alarm_light-报警灯, fan-排风扇',
  `control_name` VARCHAR(128) DEFAULT NULL COMMENT '设备显示名称（如 蜂鸣器）',
  `status` VARCHAR(32) NOT NULL DEFAULT 'off' COMMENT '当前状态：on-开启, off-关闭',
  `auto_linkage` TINYINT NOT NULL DEFAULT 1 COMMENT '是否参与自动联动：1-是, 0-否',
  `last_operated_at` DATETIME DEFAULT NULL COMMENT '最后操作时间',
  `last_operated_by` VARCHAR(64) DEFAULT NULL COMMENT '最后操作人（system 表示系统自动）',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_device_control` (`device_id`, `control_type`),
  KEY `idx_device_id` (`device_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='联动控制设备表';
```

**初始化数据**

```sql
INSERT INTO `device_control` (`device_id`, `control_type`, `control_name`, `status`) VALUES
('SMK-001', 'buzzer', '蜂鸣器', 'off'),
('SMK-001', 'alarm_light', '报警灯', 'off'),
('SMK-001', 'fan', '排风扇', 'off');
```

**设计要点**

- 一个烟感设备 (`smoke_device`) 对应多个联动设备 (`device_control`)，1:N 关系。
- `auto_linkage` 用于标识该设备是否在告警时自动联动开启。
- 危险状态自动联动逻辑：当 `alarm_record` 插入 medium/high 级别告警时，后端自动将所有 `auto_linkage=1` 的设备 status 改为 `on`，并写入操作记录。

---

## 7. 系统设置表 `system_setting`

全局配置，KV 形式存储。原型中「系统设置」菜单下的阈值配置等。

```sql
CREATE TABLE `system_setting` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `setting_key` VARCHAR(128) NOT NULL COMMENT '配置项键名',
  `setting_value` VARCHAR(512) NOT NULL COMMENT '配置项值',
  `setting_group` VARCHAR(64) DEFAULT 'general' COMMENT '配置分组：threshold-阈值, general-通用, mqtt-MQTT配置',
  `description` VARCHAR(255) DEFAULT NULL COMMENT '配置说明',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_key` (`setting_key`),
  KEY `idx_group` (`setting_group`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='系统设置表';
```

**初始化数据**

```sql
INSERT INTO `system_setting` (`setting_key`, `setting_value`, `setting_group`, `description`) VALUES
('warning_threshold', '200', 'threshold', '中风险阈值（ppm），≥此值进入中风险'),
('danger_threshold', '400', 'threshold', '高风险阈值（ppm），≥此值进入高风险'),
('unit', 'ppm', 'threshold', '浓度单位'),
('heartbeat_timeout', '300', 'general', '设备心跳超时时间（秒），超过则判定离线'),
('data_retention_days', '30', 'general', '原始烟雾数据保留天数'),
('system_name', '智慧烟感预警系统', 'general', '系统显示名称');
```

**设计要点**

- KV 结构灵活，新增配置无需改表结构。
- 阈值配置在应用启动时加载到内存缓存，修改时刷新缓存。

---

## 8. 视觉复核表 `vision_check`（加分项/P2）

AI 摄像头复核结果。告警触发后自动调用摄像头截图分析。

```sql
CREATE TABLE `vision_check` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `alarm_id` VARCHAR(64) NOT NULL COMMENT '关联告警编号',
  `device_id` VARCHAR(64) NOT NULL COMMENT '设备编号',
  `image_url` VARCHAR(512) NOT NULL COMMENT '摄像头截图URL',
  `ai_result` VARCHAR(64) DEFAULT NULL COMMENT 'AI识别结果：smoke_detected-检测到烟雾, fire_detected-检测到明火, none-无异常',
  `confidence` DECIMAL(3,2) DEFAULT NULL COMMENT '置信度（0.00-1.00）',
  `confirmed` TINYINT NOT NULL DEFAULT 0 COMMENT '是否人工确认：1-已确认, 0-未确认',
  `confirmed_by` VARCHAR(64) DEFAULT NULL COMMENT '确认人',
  `confirmed_at` DATETIME DEFAULT NULL COMMENT '确认时间',
  `checked_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '复核时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_alarm_id` (`alarm_id`),
  KEY `idx_device_id` (`device_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='AI视觉复核记录表';
```

---

## 9. 表关系图（ER 关系）

```
┌─────────────────┐       ┌──────────────────┐       ┌─────────────────┐
│   sys_user      │       │  smoke_device    │       │ device_control  │
├─────────────────┤       ├──────────────────┤       ├─────────────────┤
│ PK id           │       │ PK id            │◄──────┤ FK device_id    │
│    username     │       │    device_id     │  1:N  │    control_type │
│    role         │       │    name          │       │    status       │
│    status       │       │    location      │       │    auto_linkage │
└─────────────────┘       │    online        │       └─────────────────┘
                          │    current_...   │
                          └────────┬─────────┘
                                   │
                    ┌──────────────┼──────────────┐
                    │              │              │
                    ▼              ▼              ▼
          ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
          │   smoke_data    │ │  alarm_record   │ │  vision_check   │
          ├─────────────────┤ ├─────────────────┤ ├─────────────────┤
          │ PK id           │ │ PK id           │ │ PK id           │
          │ FK device_id    │ │ FK device_id    │ │ FK alarm_id     │
          │    smoke_value  │ │    alarm_id     │ │ FK device_id    │
          │    record_time  │ │    alarm_type   │ │    ai_result    │
          └─────────────────┘ │    smoke_value  │ │    confidence   │
                              │    risk_level   │ └─────────────────┘
                              │    status       │
                              │    handler      │       ┌─────────────────┐
                              │    triggered_at │       │ alarm_timeline  │
                              └────────┬────────┘       ├─────────────────┤
                                       │                │ PK id           │
                                       │◄───────────────┤ FK alarm_id     │
                                       │      1:N       │    event_type   │
                                       │                │    event_desc   │
                                       │                │    event_time   │
                                       │                └─────────────────┘
                                       │
                              ┌─────────────────┐
                              │ system_setting  │
                              ├─────────────────┤
                              │ PK id           │
                              │    setting_key  │
                              │    setting_value│
                              │    setting_group│
                              └─────────────────┘
```

---

## 10. 核心业务流程与 SQL 示例

### 10.1 首页总览数据查询

```sql
-- 1. 当前烟雾浓度（取最新一条）
SELECT smoke_value, risk_level, record_time 
FROM smoke_data 
WHERE device_id = 'SMK-001' 
ORDER BY record_time DESC LIMIT 1;

-- 2. 今日告警统计
SELECT 
  COUNT(*) AS today_count,
  SUM(CASE WHEN alarm_type = 'device_offline' THEN 1 ELSE 0 END) AS offline_count
FROM alarm_record 
WHERE triggered_at >= CURDATE() 
  AND triggered_at < DATE_ADD(CURDATE(), INTERVAL 1 DAY)
  AND risk_level IN ('medium', 'high');

-- 3. 设备在线数
SELECT 
  SUM(online = 1) AS online_count,
  COUNT(*) AS total_count
FROM smoke_device;

-- 4. 最近5条告警（首页表格）
SELECT alarm_id, triggered_at AS alarm_time, device_id, alarm_type, 
       smoke_value, risk_level AS level, status
FROM alarm_record 
ORDER BY triggered_at DESC 
LIMIT 5;
```

### 10.2 模拟烟雾升高（后端事务）

```sql
-- 步骤1：插入高浓度数据
INSERT INTO smoke_data (device_id, smoke_value, risk_level, record_time, source)
VALUES ('SMK-001', 520, 'high', NOW(), 'simulate');

-- 步骤2：更新设备当前状态
UPDATE smoke_device 
SET current_smoke_value = 520, current_risk_level = 'high', current_alarm_status = 'alarm', updated_at = NOW()
WHERE device_id = 'SMK-001';

-- 步骤3：生成告警记录
INSERT INTO alarm_record (alarm_id, device_id, alarm_type, smoke_value, risk_level, status, triggered_at, is_simulated)
VALUES ('ALM-20260630-001', 'SMK-001', 'smoke_high', 520, 'high', 'pending', NOW(), 1);

-- 步骤4：写入时间线
INSERT INTO alarm_timeline (alarm_id, event_type, event_desc, operator, event_time)
VALUES ('ALM-20260630-001', 'trigger', '告警触发：烟雾浓度 520 ppm', 'system', NOW());

-- 步骤5：自动联动开启设备
UPDATE device_control SET status = 'on', last_operated_at = NOW(), last_operated_by = 'system'
WHERE device_id = 'SMK-001' AND auto_linkage = 1;

-- 步骤6：记录联动事件
INSERT INTO alarm_timeline (alarm_id, event_type, event_desc, operator, extra_data, event_time)
VALUES ('ALM-20260630-001', 'linkage_start', '设备联动启动', 'system', '{"devices":["buzzer","alarm_light","fan"]}', NOW());
```

### 10.3 恢复正常环境（后端事务）

```sql
-- 步骤1：插入正常浓度数据
INSERT INTO smoke_data (device_id, smoke_value, risk_level, record_time, source)
VALUES ('SMK-001', 35, 'normal', NOW(), 'simulate');

-- 步骤2：更新设备状态
UPDATE smoke_device 
SET current_smoke_value = 35, current_risk_level = 'normal', current_alarm_status = 'safe', updated_at = NOW()
WHERE device_id = 'SMK-001';

-- 步骤3：关闭联动设备
UPDATE device_control SET status = 'off', last_operated_at = NOW(), last_operated_by = 'system'
WHERE device_id = 'SMK-001';

-- 步骤4：将当前 pending/processing 告警置为 resolved
UPDATE alarm_record 
SET status = 'resolved', resolved_at = NOW(), updated_at = NOW()
WHERE device_id = 'SMK-001' AND status IN ('pending', 'processing');
```

---

## 11. 性能优化建议

| 场景 | 优化方案 |
|------|----------|
| `smoke_data` 数据量大 | 按 `record_time` 做 Range 分区，或按月分表 |
| 首页历史趋势查询慢 | 走 `smoke_data_hourly` 聚合表，不直接查原始数据 |
| 设备心跳频繁更新 | `last_heartbeat` 更新可用缓存+批量写，减少单条 UPDATE |
| 今日告警统计 | 增加 Redis 计数器，由后端定时任务校准，避免实时 COUNT(*) |
| 告警列表分页 | `idx_triggered_at` + 倒序分页，深分页用游标/延迟关联 |

---

## 12. 扩展建议

1. **多设备扩展**：当前设计已支持多设备，若后续有「小区-楼栋-房间」层级，可增加 `building`、`floor`、`room` 表。
2. **操作日志**：增加 `operation_log` 表记录所有人工操作（谁、什么时候、操作了什么设备/告警），用于审计。
3. **消息通知**：增加 `notification` 表记录推送通知（APP推送、短信），支持重试。
4. **定时任务表**：若用轻量级调度，可增加 `scheduled_task` 表管理数据清理、聚合任务。

---

## 13. 变更记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v1.0 | 2026-06-30 | 初版，基于原型图与 API 接口文档设计 8 张核心表 |
